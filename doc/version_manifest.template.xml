<karazeh>
  
  <!--
    <identity></identity>

    These entries mark the files that will be used to validate
    the application's integrity; the accumulative checksum
    of all the files listed will be used to identify the
    application's current version.
  -->
  <identity>
    <file><![CDATA[/bin/test]]></file>
  </identity>

  <!-- 
    A manifest entry must provide: 
      1. the version's UNIQUE checksum (calculated as explained in the <identity> section)
      2. the URL to the release manifest
      3. an optional version string that will be displayed to the users but has no meaning internally
  -->
  <manifest version="0.1.0" url="/releases/test-0.1.0.xml" checksum="d41d8cd98f00b204e9800998ecf8427e" />
</karazeh>